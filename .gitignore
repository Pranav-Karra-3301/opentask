# OS
.DS_Store

# deps / builds (scaffold will extend)
node_modules/
dist/

# local data (root /data and any dev data dir, e.g. apps/server/data from `pnpm dev`)
/data/
data/
*.local
.env

# playwright artifacts (.auth dirs hold a real session — never commit)
apps/web/e2e/.auth/
apps/web/.auth/
apps/web/test-results/
apps/web/playwright-report/
.vercel
