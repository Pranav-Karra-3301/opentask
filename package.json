{
  "name": "opentask-monorepo",
  "private": true,
  "packageManager": "pnpm@10.34.5",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "build": "pnpm -r build",
    "verify": "pnpm lint && pnpm typecheck && pnpm test && pnpm openapi --check && pnpm build",
    "changeset": "changeset",
    "icons": "node scripts/generate-icons.mjs",
    "seed": "pnpm --filter @opentask/server seed",
    "screenshots": "node scripts/capture-screenshots.mjs",
    "check:bundle": "node scripts/check-bundle.mjs",
    "smoke:docker": "bash scripts/smoke-docker.sh",
    "openapi": "pnpm --filter @opentask/server exec tsx ../../scripts/generate-openapi.mjs"
  },
  "devDependencies": {
    "@biomejs/biome": "catalog:",
    "@changesets/cli": "catalog:",
    "sharp": "catalog:",
    "typescript": "catalog:"
  },
  "version": "0.5.0"
}
